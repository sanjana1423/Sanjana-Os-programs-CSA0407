int main_prog01(void) {
pid_t pid = fork();
if (pid < 0) {
perror("fork");
return 1;
}
if (pid == 0) { // child
printf("Child process: PID=%d, PPID=%d\n", getpid(), getppid());
} else { // parent
printf("Parent process: PID=%d, child PID=%d, PPID=%d\n", getpid(), pid, getppid());
}
return 0;
}
